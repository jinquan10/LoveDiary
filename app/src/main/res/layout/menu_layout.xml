<?xml version="1.0" encoding="utf-8"?>
<org.jz.lovediary.layout.MenuLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                    android:layout_width="match_parent"
                                    android:layout_height="match_parent"
                                    android:orientation="horizontal">

    <TextView
        android:id="@+id/share"
        style="@style/menuItem"
        android:text="Share"/>

    <TextView
        android:id="@+id/stats"
        style="@style/menuItem"
        android:text="R.Stats"/>

    <TextView
        android:id="@+id/tag"
        style="@style/menuItem"
        android:text="Tag 'Friend'"/>

    <TextView
        android:id="@+id/write"
        style="@style/menuItem"
        android:text="Write"
        />
</org.jz.lovediary.layout.MenuLayout>